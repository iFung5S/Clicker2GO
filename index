<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <link href='http://fonts.googleapis.com/css?family=Pacifico' rel='stylesheet' type='text/css'>
    <title>Home | Clicker2GO</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body class="homepage">
       <div class="form-wrapper">

          <h1>HOME</h1>

          <!-- GETS THE ID "greetings" in style.css -->
          <p class="normalTextStyle" id="greetings"><a href="login/profile.php">##name##</a>!</p>

          <p>##admin_panel##</p>
          <p class="textMiddle"><a href="login/logout.php">Logout</a></p>

          <br>

          <div id=rectangle> </div>
          <br/>

          <h2>Courses</h2>

          <br>

          <!-- LIST OF ALL COURSES -->
          <h2> <ul>##list_course##</ul> </h2>
          <br/>
          <form class="textMiddle" method="POST" action="questions/addCourseTaken.php">

          <!-- FOR STUDENT: SELECT COURSE -->
          ##add_course##

          <!-- ADD COURSE BUTTON -->
          <div class="button-panel">
            <input type="submit" class="button" value="Add Course"/>
          </div>

          </form>
          <br/>
      </div>

<!-- SCRIPT FOR SAYING: GOOD EVENING OR GOOD AFTERNOON -->
<script>

         var text = document.getElementById("greetings").innerHTML;
         document.getElementById("greetings").innerHTML = greetings() + text;

         function greetings()
         {
           var myDate = new Date(); 
           /* 5:00 am - 11:59 am */
           if ( myDate.getHours() >= 5 && myDate.getHours() < 12 )
           { 
             return "Good morning, ";
           } 
           /* 12:00 nn - 5:59 pm */
           else if ( myDate.getHours() >= 12 && myDate.getHours() < 19 ) 
           {
             return "Good afternoon, ";
           }
           /* 6:00 pm - 12:00 mn OR 12:00 mn - 5:00 am */
           else if ( ( myDate.getHours() >= 19 && myDate.getHours() <= 24 ) || ( myDate.getHours() >= 0 && myDate.getHours() < 5 ) )
           {
             return "Good evening, ";
           } 
           else  /* the hour is not between 0 and 24, so something is wrong */
           { 
             return "Welcome, ";
           }
         } 
</script>
  </body>
</html>
